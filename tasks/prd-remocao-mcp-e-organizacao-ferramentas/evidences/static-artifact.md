# Evidência — artefato e fronteira legada

- `dist/modules/mcp`: ausente.
- `dist/modules/municipalize-tools`: ausente.
- Guidance no artefato: 36 documentos; os 18 prompts são registrados como código nativo e foram cobertos pela suíte de guidance.
- Suíte arquitetural `native-tools-only.spec.ts`: aprovada.
- As únicas ocorrências de `modelcontextprotocol/sdk` no escopo ativo são asserções negativas dos testes; não há import, configuração, rota ou execução MCP.
